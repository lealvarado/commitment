pipeline {
     agent any
     stages {
          stage("Checkout") {
               steps {
                    git url: 'https://github.com/lealvarado/polling.git'
               }
          }
     }
}
